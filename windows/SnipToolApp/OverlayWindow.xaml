<Window x:Class="SnipToolApp.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowState="Maximized"
        Topmost="True"
        ShowInTaskbar="False"
        KeyDown="Window_KeyDown">
    <Canvas Background="#50000000" MouseDown="Overlay_MouseDown" MouseMove="Overlay_MouseMove" MouseUp="Overlay_MouseUp">
        <Image x:Name="PreviewImage" Stretch="UniformToFill" Opacity="0.35" Canvas.Left="0" Canvas.Top="0" Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Window}}" Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Window}}" />
        <Rectangle x:Name="SelectionRect" Stroke="#FF5BC0FF" StrokeThickness="2" Fill="#405BC0FF" Visibility="Collapsed" />
        <TextBlock Text="Drag to capture a region. Press Esc to cancel." Foreground="White" FontSize="16" Canvas.Left="50" Canvas.Top="24" />
    </Canvas>
</Window>
