<Window x:Class="AutoMarkerReID.App.ImageEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chỉnh sửa ảnh" Width="1100" Height="760" MinWidth="760" MinHeight="520"
        WindowStartupLocation="CenterScreen" KeyDown="OnKeyDown"
        PreviewMouseMove="OnMouseMove" PreviewMouseLeftButtonUp="OnMouseUp"
        Background="{StaticResource WindowBackgroundBrush}" Foreground="{StaticResource TextBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <WrapPanel Margin="0,0,0,10">
            <RadioButton x:Name="CropMode" Content="Cắt lấy vùng chọn" ToolTip="Chỉ giữ lại phần ảnh nằm trong vùng chọn" IsChecked="True"/>
            <RadioButton x:Name="CutMode" Content="Xóa một dải" ToolTip="Loại bỏ một dải ngang hoặc dọc khỏi ảnh"/>
            <Button Content="Hoàn tác" Click="OnUndo"/>
            <Button Content="Đặt lại" Click="OnReset"/>
            <Button Content="Ghép ảnh bên trái" Click="OnMergeLeft"/>
            <Button Content="Ghép ảnh bên phải" Click="OnMergeRight"/>
        </WrapPanel>
        <Border x:Name="EditorViewport" Grid.Row="1" Background="#090F1B" CornerRadius="8" ClipToBounds="True"
                MouseLeftButtonDown="OnMouseDown">
            <Viewbox Stretch="Uniform" StretchDirection="DownOnly"
                     HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid x:Name="ImageSurface">
                    <Image x:Name="PreviewImage" Stretch="None"/>
                    <Canvas x:Name="Overlay" Background="Transparent">
                        <Rectangle x:Name="Selection" Visibility="Collapsed" Stroke="#38BDF8" StrokeThickness="2" Fill="#2238BDF8"/>
                    </Canvas>
                </Grid>
            </Viewbox>
        </Border>
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock x:Name="InfoText" VerticalAlignment="Center" Foreground="{StaticResource MutedTextBrush}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="HỦY" Click="OnCancel"/>
                <Button Content="LƯU" Background="#0284C7" Click="OnSave"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
